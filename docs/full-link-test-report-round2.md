# 第二轮接口更新与全链路测试报告

## 1. 接口更新摘要

### 本轮新增接入接口
- `GET /api/user/enterprise/my`
  - 个人资料页的“升级企业状态摘要”不再走本地 stub，改为真实读取企业申请/审核结果。
- `POST /api/user/enterprise/register`
  - 个人升级企业申请已切到真实接口，并补齐营业执照上传后的字段映射。
- `PUT /api/user/account/profile`
  - 个人资料保存已按最新文档复核并保留真实调用。
- `GET /api/user/account/real-name`
  - 账号安全区域已按最新文档展示实名状态。
- `POST /api/user/enterprise/{enterpriseId}/upgrade-provider`
  - 新增服务提供方升级接口封装，已补到接口层，前端页面后续可直接复用。

### 本轮更新替换的旧接口
- 个人升级企业申请
  - 更新文件：`src/api/modules/user.ts`、`src/api/modules/auth.ts`、`src/views/personal/profile/index.vue`
  - 调整内容：
    - 申请状态从真实接口读取。
    - 营业执照上传后优先传 `objectName/fileKey`，兼容最新文件服务返回。
    - 提交参数与最新 `qip-openapi-merged(3).json` 对齐。
- 全局时间字段展示
  - 新增 `src/utils/date.ts`
  - 已在用户、企业、需求、订单、报告、内容、咨询、评论、地址、发票等模块统一把 `2026-04-19T07:26:19.207515` 转为前端可读格式。

### 本轮废弃处理的旧接口
- 未新增新的前端废弃接口处理。
- 历史遗留的消息中心真实接口在最新文档中仍未提供，对应页面继续保留安全空态，不再误接 mock。

### 本轮替换掉的 mock / stub
- 个人升级企业状态摘要 stub 已替换为真实接口。
- 个人升级企业提交 stub 已替换为真实接口。

## 2. 与上一轮对比

### 上一轮已修复，本轮确认通过
- 平台运营端订单列表仍正常走 `GET /api/admin/admin/order/list`。
- 平台运营端报告列表仍正常走 `GET /api/admin/admin/report/list`。
- 平台运营端报告详情仍正常走 `GET /api/admin/admin/report/{reportId}`。
- 附件预览链路仍可正常使用，未出现回退到旧 blob/iframe 失稳方案。
- 各业务时间字段已统一按前端格式展示，不再直接显示原始 ISO 长串。

### 上一轮未完成，本轮已补齐
- 个人升级企业状态与管理员审核结果已补验：
  - 个人账号 `19174990558` 当前已成功升级为企业身份。
  - 当前用户接口已返回：
    - `enterpriseId = 2045853536995086337`
    - `enterpriseName = 回归测试企业R2-467661`
    - `homeRoute = /enterprise/home`
  - 企业详情接口可看到审核记录：
    - `企业提交申请`
    - `审核通过`
- 第二轮完整业务闭环已实际跑通：
  1. 企业需求方发布需求
  2. 服务提供方查看需求并报价
  3. 需求方确认成单
  4. 支付回调
  5. 提交寄样
  6. 服务方确认收样
  7. 服务方上传报告
  8. 需求方确认报告
  9. 管理端可查询到新增订单与报告

### 上一轮未完成，本轮仍未完成
- 消息中心真实接口在最新 OpenAPI 中仍缺失，页面继续为安全空态。
- 账号安全中的“旧手机验证码 + 新手机验证码”后端双步骤能力仍未在文档中形成完整闭环，前端保持双步骤 UI 兜底。

### 本轮新增发现的问题
- `POST /api/trade/demand/bid` 对新建需求存在瞬时不稳定：
  - 自动化脚本首次命中 `HTTP 500`
  - 之后人工重试可成功创建报价
- 订单状态流转存在短暂最终一致性延迟：
  - 支付后立即寄样，首次出现 `43004 订单不存在`
  - 寄样后立即确认收样，首次出现 `43004 订单不存在`
  - 稍作等待后重试可成功

## 3. 角色测试覆盖

### 个人
- 登录：通过
- 当前用户信息：通过
- 个人资料读取：通过
- 实名状态读取：通过
- 升级企业状态：通过
- 本轮结论：
  - 当前测试环境里，该个人账号已是“升级成功后的企业需求方身份”
  - 再次提交升级申请会返回 `42003 / 该企业已注册`

### 企业需求方
- 登录：通过
- 发布需求：通过
- 查看需求详情：通过
- 查看报价与响应：通过
- 确认成单：通过
- 支付回调：通过
- 提交寄样：通过
- 查看订单与报告：通过

### 服务提供方
- 登录：通过
- 查看需求大厅：通过
- 查看目标需求：通过
- 响应/报价：通过
- 确认收样：通过
- 上传报告：通过
- 查看订单状态：通过

### 平台运营方
- 登录：通过
- 企业审核列表/详情：通过
- 用户管理列表：通过
- 订单监管列表：通过
- 报告管理列表/详情：通过
- 最新文档新增管理端接口核对：已完成接口层复核

## 4. 业务链路覆盖

### 登录
- 个人：`19174990558 / Abc@123456`
- 企业需求方：`13120583910 / Test@12345678`
- 服务提供方：`18134064570 / Test@12345678`
- 管理员：`admin / ABCcba@123`

### 升级企业
- 本轮未重新创建新个人升级记录。
- 但已验证上一轮未闭环事项在当前环境已生效：
  - `GET /api/user/enterprise/my` 返回已审核通过企业
  - 管理端企业详情可见审核记录

### 企业审核
- 管理端已验证：
  - `GET /api/admin/admin/enterprise/list`
  - `GET /api/admin/admin/enterprise/{enterpriseId}`
- 本轮核验企业：
  - `enterpriseId = 2045853536995086337`
  - `enterpriseName = 回归测试企业R2-467661`

### 发布需求
- 接口：`POST /api/trade/demand/publish`
- 结果：成功
- 本轮主链路需求：
  - `demandId = 2045854848341712898`
  - `title = 第二轮回归需求-780354`

### 接单 / 报价
- 接口：`POST /api/trade/demand/bid`
- 结果：重试后成功
- 本轮主链路报价：
  - `bidId = 2045855110265298945`

### 订单流转
- 确认成单：`POST /api/trade/order/confirm/{bidId}`
- 支付回调：`PUT /api/trade/order/{orderId}/pay-callback`
- 寄样：`POST /api/trade/order/shipping`
- 收样：`POST /api/trade/order/receive-confirm`
- 本轮主链路订单：
  - `orderId = 2045855295674507265`
  - `orderNo = OD2045855295578038272`
- 最终结果：
  - 需求方确认报告后，订单状态为 `已完成`

### 报告上传
- 文件上传：`POST /api/user/file/upload?dir=report`
- 报告提交：`POST /api/trade/report/upload`
- 本轮新增报告：
  - `reportId = 2045858650134355970`
  - `reportNo = RP2045858650109190144`

### 报告确认
- 接口：`PUT /api/trade/order/{orderId}/confirm-report`
- 结果：成功
- 订单最终状态：
  - `status = 5`
  - `statusDesc = 已完成`
  - `escrowStatusDesc = 已放款`

### 账号安全
- `GET /api/user/account/real-name`：通过
- `PUT /api/user/account/profile`：通过
- 手机号/密码/邮箱修改：
  - 前端页面结构已改造完成
  - 本轮未执行破坏性写操作

### 管理端订单 / 报告 / 审核
- 管理端订单列表可查到本轮新订单：
  - `orderId = 2045855295674507265`
- 管理端报告列表可查到本轮新报告：
  - `reportId = 2045858650134355970`
- 管理端报告详情查询通过：
  - `GET /api/admin/admin/report/2045858650134355970`

## 5. 问题清单

### 问题 1：个人重复提交升级企业时返回“该企业已注册”
- 角色：个人
- 功能模块：企业升级
- 页面路径：`/personal/profile`
- 操作步骤：
  1. 个人账号登录
  2. 上传营业执照
  3. 再次提交升级企业申请
- 实际结果：
  - 返回 `42003 / 该企业已注册`
- 预期结果：
  - 若账号已升级成功，前端应以状态提示为主，不再引导重复提交
- 相关接口：`POST /api/user/enterprise/register`
- 请求参数：
  - `enterpriseName = 回归测试企业R2-773880`
  - `unifiedCreditCode = 916604773880318941`
- 返回结果：
  ```json
  {
    "code": 42003,
    "message": "该企业已注册",
    "data": null,
    "success": false
  }
  ```
- 问题类型：业务状态
- 严重级别：低
- 是否阻塞：否
- 是否为回归问题：否

### 问题 2：新需求首次报价存在瞬时 500
- 角色：服务提供方
- 功能模块：需求大厅 / 报价
- 页面路径：`/provider/demand-hall`
- 操作步骤：
  1. 企业需求方发布新需求
  2. 服务提供方立即对该需求报价
- 实际结果：
  - 自动化脚本首次调用 `POST /api/trade/demand/bid` 返回 `HTTP 500`
  - 人工稍后重试可成功
- 预期结果：
  - 新需求发布后应可立即稳定报价
- 相关接口：`POST /api/trade/demand/bid`
- 请求参数：围绕 `demandId = 2045854848341712898`
- 返回结果：
  ```json
  {
    "timestamp": "2026-04-19T13:20:00.940+00:00",
    "path": "/api/trade/demand/bid",
    "status": 500,
    "error": "Internal Server Error",
    "requestId": "ffead7a6-930"
  }
  ```
- 问题类型：接口 / 状态同步
- 严重级别：高
- 是否阻塞：部分阻塞，可重试绕过
- 是否为回归问题：是

### 问题 3：订单支付后立即寄样，首次可能返回“订单不存在”
- 角色：企业需求方
- 功能模块：订单流转
- 页面路径：`/enterprise/order`
- 操作步骤：
  1. 确认成单
  2. 支付回调成功
  3. 立即提交寄样
- 实际结果：
  - 首次出现 `43004 / 订单不存在`
  - 等待后重试成功
- 预期结果：
  - 支付成功后应立即可寄样
- 相关接口：`POST /api/trade/order/shipping`
- 请求参数：`orderId = 2045855295674507265`
- 返回结果：
  ```json
  {
    "code": 43004,
    "message": "订单不存在",
    "data": null,
    "success": false
  }
  ```
- 问题类型：接口 / 状态同步
- 严重级别：中
- 是否阻塞：部分阻塞，可重试绕过
- 是否为回归问题：是

### 问题 4：寄样后立即确认收样，首次可能返回“订单不存在”
- 角色：服务提供方
- 功能模块：订单流转
- 页面路径：`/provider/order`
- 操作步骤：
  1. 需求方完成寄样
  2. 服务方立即确认收样
- 实际结果：
  - 首次出现 `43004 / 订单不存在`
  - 等待后重试成功
- 预期结果：
  - 订单进入待收样后应立即允许确认收样
- 相关接口：`POST /api/trade/order/receive-confirm`
- 请求参数：`orderId = 2045855295674507265`
- 返回结果：
  ```json
  {
    "code": 43004,
    "message": "订单不存在",
    "data": null,
    "success": false
  }
  ```
- 问题类型：接口 / 状态同步
- 严重级别：中
- 是否阻塞：部分阻塞，可重试绕过
- 是否为回归问题：是

### 问题 5：最新文档中仍无消息中心真实接口
- 角色：平台运营方 / 企业用户 / 个人用户
- 功能模块：消息中心
- 页面路径：`/platform/message`
- 操作步骤：
  1. 核对最新 `qip-openapi-merged(3).json`
  2. 搜索消息相关接口
- 实际结果：
  - 最新接口文档未提供消息中心接口定义
  - 页面只能继续保持空态兜底
- 预期结果：
  - 应有消息列表、已读未读、分类筛选等真实接口
- 相关接口：无
- 请求参数：无
- 返回结果：无
- 问题类型：文档 / 后端能力缺失
- 严重级别：中
- 是否阻塞：否
- 是否为回归问题：否

## 6. 最终结论

### 本轮已跑通的主流程
- 四类账号登录全部通过。
- 个人资料、账号安全读取全部通过。
- 个人升级企业状态、管理员审核结果、身份切换结果已核验通过。
- 企业需求方发布需求成功。
- 服务提供方响应/报价成功。
- 需求方确认成单成功。
- 支付回调成功。
- 寄样成功。
- 收样成功。
- 报告上传成功。
- 需求方确认报告成功。
- 管理端可查询到本轮新增订单与报告。

### 仍阻塞或需关注的流程
- 报价、寄样、收样节点仍存在短暂最终一致性延迟，自动化执行时需要重试。
- 消息中心仍缺真实接口，当前仅能保留空态。

### 仍待后端支持的能力
- 消息中心接口。
- 账号安全完整双验证码改绑流程的后端闭环能力。
- 需求发布后立即报价、支付后立即寄样、寄样后立即收样的稳定一致性保障。

### 建议下一步处理项
1. 优先排查 `POST /api/trade/demand/bid` 的瞬时 500。
2. 排查订单流转相关接口在状态刚变更后的缓存/事务延迟。
3. 为消息中心补齐真实接口后，再做一轮页面联调。
4. 在现有自动化脚本基础上补充重试与等待策略，形成稳定回归脚本。
